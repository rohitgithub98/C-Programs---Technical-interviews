#include<stdio.h>
int main()
{
	int i,j,k;
	for(i=1;i<=20;i++)
	{
		for(j=1;j<=20;j++)
		{
			for(k=1;k<=20;k++)
			{
				if((i+j+k==20) && (((i*7)-(2*j))==87))
				{
					printf("%5d%5d%5d",i,j,k);
					return 0;
				}
			}
		}
	}
}
